---
loglevel: info
server: tcp://media:1705
streams:
  {{#hosts}}
  {{Name}}:
    clients:
      - {{name}}
    volume:
      {{name}}: 100
  {{/hosts}}

  {{#party-zones}}
  {{Name}}:
    clients:
      {{#clients}}
      - {{.}}
      {{/clients}}
    volume:
      {{#clients}}
      {{.}}: 100
      {{/clients}}
  {{/party-zones}}
